function NewPopulation = ZSA(Population,TObjs,Z)
%ZSA 此处显示有关此函数的摘要
%   此处显示详细说明
    tmpObjs = TObjs(:,end);
    NewPopulation = INDIVIDUAL;
    AZ = unique(Z);
    for i = 1:length(AZ)
        AZ(i);
    end
end

